# Resume 2

This is the tightened, recruiter-facing version of Dimitri Chrysafis's resume.

- `main.tex` is the editable source.
- `main.pdf` is the compiled resume.
- The original resume in the parent directory is unchanged.

Build with `latexmk -pdf main.tex`.
