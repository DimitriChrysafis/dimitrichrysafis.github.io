{
  "name": "analytics-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute site_analytics --file=./schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
